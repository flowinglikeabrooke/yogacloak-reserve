{
  "name": "yogacloak-reserve",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "seed": "node -e \"import('./lib/availability.js').then(m => m.seedAvailability())\""
  },
  "dependencies": {
    "stripe": "^14.0.0",
    "@vercel/kv": "^2.0.0"
  },
  "devDependencies": {
    "vercel": "^34.0.0"
  }
}
